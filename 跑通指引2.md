# emoSVG 跑通指引 2：Cartoon Rig 版本

本文是当前阶段 1-5 改造后的新版跑通指引。它和旧版服务器跑通指南的侧重点不同：

- 旧版重点：安装 LivePortrait、ToonCrafter、TripoSR、SAM 等真实模型，把原始全管线跑通。
- 本版重点：先跑通最新的 Q 版角色诊断、图层解析、`cartoon_rig` 显式 2D 动画、debug 可视化、动画评估，再按需启用 ToonCrafter/LivePortrait/3D/SVG。

推荐顺序：

1. 先跑通 Python 环境和依赖。
2. 先跑通 `cartoon_rig`，验证 Q 版角色动画新路线。
3. 再跑 debug 诊断，看五官 bbox、mask、图层是否合理。
4. 再启动 FastAPI，用 `/animate backend=cartoon_rig` 生成 GIF。
5. 最后按需启用 ToonCrafter 后处理、LivePortrait、TripoSR、SAM。

## 0. 当前新系统做了什么

阶段 1-5 后，项目新增了以下能力：

```text
输入图片
  -> Q 版五官几何诊断 CartoonFaceAnalyzer
  -> 粗粒度图层解析 CartoonLayerParser
  -> debug 可视化 PipelineDebugWriter
  -> 显式 2D rig CartoonRigBuilder / CartoonRigAnimator
  -> 可选 ToonCrafter rig 后处理插帧
  -> 动画指标 evaluate_animation_frames
  -> API 返回 metrics/debug_dir/cartoon_confidence/cartoon_layer_count
```

现在 `/animate`、`/generate`、`/batch/generate` 都支持：

```text
backend=live_portrait
backend=cartoon_rig
backend=auto
debug=true
```

其中 `cartoon_rig` 是为 Q 版表情包角色新增的主推后端。

## 1. 推荐环境原则

### 1.1 不要再全量安装第三方 requirements

不要直接执行：

```bash
pip install -r ~/workspace/third_party/LivePortrait/requirements.txt
pip install -r ~/workspace/third_party/ToonCrafter/requirements.txt
```

原因是这些 requirements 容易把主环境里的核心包改坏，例如：

```text
torch / torchvision
numpy
opencv-python / opencv-contrib-python / opencv-python-headless
Pillow
imageio
protobuf
transformers
setuptools
xformers
gradio
```

推荐方式是：

- 主环境固定核心版本。
- 第三方源码通过 `.pth` 挂路径。
- 缺什么小包补什么小包。
- ToonCrafter 真实模型如果冲突严重，可以之后单独环境服务化。

### 1.2 推荐 conda 环境

如果之前环境已经被污染，建议新建环境：

```bash
conda deactivate
conda create -n EmoSVG python=3.10 -y
conda activate EmoSVG

cd ~/workspace/emoSVG
python -m pip install --upgrade pip setuptools wheel
```

如果当前 `EmoSVG` 环境已经能跑通，也可以继续使用，不必重建。

## 2. 安装主项目依赖

### 2.1 安装 PyTorch

CUDA 12.1/12.x 优先：

```bash
pip install torch==2.3.0 torchvision==0.18.0 --index-url https://download.pytorch.org/whl/cu121
```

CUDA 11.8：

```bash
pip install torch==2.3.0 torchvision==0.18.0 --index-url https://download.pytorch.org/whl/cu118
```

验证：

```bash
python - <<'PY'
import torch, torchvision
print("torch:", torch.__version__)
print("torchvision:", torchvision.__version__)
print("cuda available:", torch.cuda.is_available())
print("torch cuda:", torch.version.cuda)
if torch.cuda.is_available():
    print("gpu:", torch.cuda.get_device_name(0))
PY
```

### 2.2 安装 emoSVG 主依赖

```bash
cd ~/workspace/emoSVG
pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu121
pip install -e .
```

如果是 CUDA 11.8，把 `cu121` 换成 `cu118`。

### 2.3 固定 NumPy 和 OpenCV

这是之前烟雾测试遇到问题后确认过的稳定组合：

```bash
pip install "numpy==1.26.4" -i https://pypi.tuna.tsinghua.edu.cn/simple
pip uninstall opencv-contrib-python opencv-python opencv-python-headless -y
pip install opencv-python-headless==4.9.0.80 -i https://pypi.tuna.tsinghua.edu.cn/simple
```

注意：先看到 `opencv-contrib-python requires numpy>=2` 的 warning 没关系，因为下一步会卸掉 contrib 版本并装回 headless 4.9。

验证：

```bash
python - <<'PY'
import cv2
import numpy as np
import torch
import PIL
print("cv2:", cv2.__version__)
print("numpy:", np.__version__)
print("torch:", torch.__version__, "cuda:", torch.cuda.is_available())
print("PIL:", PIL.__version__)
PY
```

## 3. 第三方源码挂路径

如果你已经有这些源码：

```text
~/workspace/third_party/segment-anything
~/workspace/third_party/TripoSR
~/workspace/third_party/LivePortrait
~/workspace/third_party/ToonCrafter
```

则不要全量安装 requirements，优先挂路径。

### 3.1 TripoSR 和 ToonCrafter

```bash
SITE_PACKAGES=$(python - <<'PY'
import site
print(site.getsitepackages()[0])
PY
)

echo "$HOME/workspace/third_party/TripoSR" > "$SITE_PACKAGES/emosvg_triposr.pth"
echo "$HOME/workspace/third_party/ToonCrafter" > "$SITE_PACKAGES/emosvg_tooncrafter.pth"
```

如果目录名带 `-main`，按实际目录改：

```bash
echo "$HOME/workspace/third_party/TripoSR-main" > "$SITE_PACKAGES/emosvg_triposr.pth"
echo "$HOME/workspace/third_party/ToonCrafter-main" > "$SITE_PACKAGES/emosvg_tooncrafter.pth"
```

### 3.2 LivePortrait 兼容包壳

LivePortrait 官方源码常见结构是：

```text
LivePortrait/src/live_portrait_pipeline.py
LivePortrait/src/config/inference_config.py
```

它内部使用相对导入，所以需要包一层：

```bash
mkdir -p ~/workspace/third_party/liveportrait_compat
cp -a ~/workspace/third_party/LivePortrait/src \
  ~/workspace/third_party/liveportrait_compat/liveportrait
touch ~/workspace/third_party/liveportrait_compat/liveportrait/__init__.py

echo "$HOME/workspace/third_party/liveportrait_compat" > "$SITE_PACKAGES/emosvg_liveportrait.pth"
```

如果目录是 `LivePortrait-main`：

```bash
mkdir -p ~/workspace/third_party/liveportrait_compat
cp -a ~/workspace/third_party/LivePortrait-main/src \
  ~/workspace/third_party/liveportrait_compat/liveportrait
touch ~/workspace/third_party/liveportrait_compat/liveportrait/__init__.py

echo "$HOME/workspace/third_party/liveportrait_compat" > "$SITE_PACKAGES/emosvg_liveportrait.pth"
```

### 3.3 segment-anything

```bash
pip install ~/workspace/third_party/segment-anything
```

如果目录是 `segment-anything-main`：

```bash
pip install ~/workspace/third_party/segment-anything-main
```

### 3.4 补小包

如果 import 报小包缺失，优先单独补：

```bash
pip install tyro pykalman lmdb ffmpeg-python -i https://pypi.tuna.tsinghua.edu.cn/simple
pip install decord omegaconf pytorch-lightning open_clip_torch timm av moviepy -i https://pypi.tuna.tsinghua.edu.cn/simple
```

如果出现：

```text
ModuleNotFoundError: No module named 'pkg_resources'
```

执行：

```bash
pip install "setuptools<70" -i https://pypi.tuna.tsinghua.edu.cn/simple
```

## 4. `.env` 环境变量

推荐在项目根目录创建或更新 `.env`：

```bash
cd ~/workspace/emoSVG
cat > .env <<'EOF'
MODELS_ROOT=/data3/zhengmuhan/workspace/emosvg_models
OUTPUT_ROOT=./outputs

HF_ENDPOINT=https://hf-mirror.com
HF_HOME=/data3/zhengmuhan/workspace/hf_cache
TRANSFORMERS_CACHE=/data3/zhengmuhan/workspace/hf_cache

LIVE_PORTRAIT_MODEL_PATH=/data3/zhengmuhan/workspace/emosvg_models/live_portrait
TRIPOSR_MODEL_PATH=/data3/zhengmuhan/workspace/emosvg_models/triposr
TOON_CRAFTER_MODEL_PATH=/data3/zhengmuhan/workspace/emosvg_models/toon_crafter
TOON_CRAFTER_REPO_PATH=/data3/zhengmuhan/workspace/third_party/ToonCrafter
SAM_MODEL_PATH=/data3/zhengmuhan/workspace/emosvg_models/sam/sam_vit_h_4b8939.pth

TRIPOSR_FORCE_FLOAT32=1
NO_ALBUMENTATIONS_UPDATE=1

# Cartoon analyzer:
# auto = prefer trained anime-face-detector, fallback to heuristic if unavailable.
# anime_face_detector = use trained 28-point anime landmark model.
# heuristic = use deterministic color/geometry fallback.
CARTOON_ANALYZER_BACKEND=auto
CARTOON_ANALYZER_DEVICE=cuda:0
CARTOON_ANALYZER_MODEL=yolov3
CARTOON_ANALYZER_ALLOW_HEURISTIC_FALLBACK=1
EOF
```

每次新开终端后，需要加载：

```bash
set -a
source .env
set +a
```

解释：

- `source .env`：把 `.env` 里的变量读进当前 shell。
- `set -a`：让后续变量自动 export，Python 子进程才能读到。
- `set +a`：关闭自动 export。

验证：

```bash
python - <<'PY'
import os
for k in [
    "HF_ENDPOINT",
    "MODELS_ROOT",
    "LIVE_PORTRAIT_MODEL_PATH",
    "TRIPOSR_MODEL_PATH",
    "TOON_CRAFTER_MODEL_PATH",
    "TOON_CRAFTER_REPO_PATH",
    "SAM_MODEL_PATH",
]:
    print(k, "=", os.getenv(k))
PY
```

如果输出是 `None`，说明 `.env` 没有被正确加载。

## 5. 验证第三方 import

```bash
python - <<'PY'
checks = []

try:
    import segment_anything
    checks.append(("segment_anything", "OK"))
except Exception as e:
    checks.append(("segment_anything", repr(e)))

try:
    import tsr
    checks.append(("TripoSR tsr", "OK"))
except Exception as e:
    checks.append(("TripoSR tsr", repr(e)))

try:
    from liveportrait.live_portrait_pipeline import LivePortraitPipeline
    checks.append(("LivePortrait", "OK"))
except Exception as e:
    checks.append(("LivePortrait", repr(e)))

try:
    import lvdm
    checks.append(("ToonCrafter lvdm", "OK"))
except Exception as e:
    checks.append(("ToonCrafter lvdm", repr(e)))

for name, result in checks:
    print(f"{name}: {result}")
PY
```

期望：

```text
segment_anything: OK
TripoSR tsr: OK
LivePortrait: OK
ToonCrafter lvdm: OK
```

## 6. 权重状态检查

### 6.1 只跑 cartoon_rig 需要哪些权重

如果只跑：

```text
backend=cartoon_rig
run_3d=false
run_svg=false
use_toon_crafter=false
```

理论上不需要 LivePortrait、ToonCrafter、TripoSR、SAM 权重。

但当前 `FullPipeline` 仍会执行 IP extraction，所以第一次 API 请求可能会加载或下载 CLIP：

```text
openai/clip-vit-large-patch14
```

如果服务器访问 HuggingFace 慢，请先设置：

```bash
export HF_ENDPOINT=https://hf-mirror.com
```

或者确保 `.env` 已经加载。

### 6.2 检查真实权重

```bash
ls -lh "$TRIPOSR_MODEL_PATH/config.yaml" "$TRIPOSR_MODEL_PATH/model.ckpt"
ls -lh "$TOON_CRAFTER_MODEL_PATH/model.ckpt" "$TOON_CRAFTER_MODEL_PATH/config.yaml"
ls -lh "$SAM_MODEL_PATH"
find "$LIVE_PORTRAIT_MODEL_PATH" -maxdepth 6 -type f | sort | grep -E "appearance|motion|warping|spade|stitching|retargeting"
```

如果 SAM 不存在：

```bash
mkdir -p "$MODELS_ROOT/sam"
cd "$MODELS_ROOT/sam"
wget -c https://dl.fbaipublicfiles.com/segment_anything/sam_vit_h_4b8939.pth
```

如果 LivePortrait 不存在，推荐：

```bash
HF_ENDPOINT=https://hf-mirror.com huggingface-cli download KwaiVGI/LivePortrait \
  --repo-type model \
  --local-dir "$LIVE_PORTRAIT_MODEL_PATH" \
  --resume-download
```

如果 TripoSR 不存在：

```bash
HF_ENDPOINT=https://hf-mirror.com python scripts/download_models.py --models triposr
```

如果 ToonCrafter 不存在：

```bash
HF_ENDPOINT=https://hf-mirror.com python scripts/download_models.py --models toon_crafter
```

## 7. 最小跑通：不启真实模型，先跑 cartoon diagnostics

这一步用来验证阶段 1/2 是否正常：

```bash
cd ~/workspace/emoSVG
conda activate EmoSVG
set -a
source .env
set +a

python scripts/cartoon_diagnostics.py \
  test_data/1.png \
  --output-root outputs/debug_cartoon_1
```

默认 `CARTOON_ANALYZER_BACKEND=auto` 会优先尝试训练模型 `hysts/anime-face-detector`，如果当前环境没安装它，会自动回退到启发式后端。返回 JSON 里的 `backend_used` 会告诉你实际用了哪个后端。

如果只想跑旧的启发式后端：

```bash
python scripts/cartoon_diagnostics.py \
  test_data/1.png \
  --output-root outputs/debug_cartoon_heuristic \
  --backend heuristic
```

如果已经安装了 `anime-face-detector`，建议强制使用训练模型，并禁止静默回退：

```bash
python scripts/cartoon_diagnostics.py \
  test_data/1.png \
  --output-root outputs/debug_cartoon_anime_detector \
  --backend anime_face_detector \
  --device cuda:0 \
  --strict-trained
```

期望 JSON 里出现：

```text
"backend_used": "anime_face_detector"
```

如果报缺包，说明当前环境还没有安装 `hysts/anime-face-detector` 及 OpenMMLab 依赖。

### 7.1 安装训练版动漫五官检测器

`anime-face-detector` 依赖较老的 OpenMMLab 生态，建议优先单独环境或谨慎装入主环境。若要直接在当前 `EmoSVG` 环境尝试，先备份环境导出：

```bash
conda env export > emosvg_before_anime_face_detector.yml
```

然后按 `hysts/anime-face-detector` 官方 README 安装。装好后验证：

```bash
python - <<'PY'
from anime_face_detector import create_detector
detector = create_detector("yolov3", device="cuda:0")
print("anime-face-detector OK")
PY
```

再跑：

```bash
python scripts/cartoon_diagnostics.py \
  /data3/zhengmuhan/workspace/emoSVG/test_data/1.png \
  --output-root outputs/debug_stage012_anime_detector \
  --backend anime_face_detector \
  --device cuda:0 \
  --strict-trained
```

如果你不想污染主环境，可以把它做成后续独立服务；当前代码已经把后端接口隔离在 `src/modules/cartoon_analyzer/anime_face_detector_adapter.py`，之后改成 HTTP/RPC 调用也比较直接。

期望输出目录里有：

```text
input.png
landmark_overlay.png
masks_overlay.png
layers/
metrics.json
```

重点检查：

- `landmark_overlay.png`：眼睛框和嘴巴框有没有框在正确位置。
- `masks_overlay.png`：左眼、右眼、嘴巴 mask 是否大致覆盖目标。
- `layers/left_eye.png`、`layers/right_eye.png`、`layers/mouth.png` 是否可用。

如果这里已经错了，后面的动画质量通常也会差。应优先修 `CartoonFaceAnalyzer` 或手动检查图像是否过度裁切。

## 8. 最小跑通：直接跑 cartoon_rig 动画

可以先走 Python 内部调用，不启动 API：

```bash
python - <<'PY'
from pathlib import Path
from src.modules.meme_animator.animator import MemeAnimator
from src.modules.meme_animator.schemas import AnimationRequest, AnimationBackend, MemeExpression

image = Path("test_data/1.png")
animator = MemeAnimator(output_dir=Path("outputs/animations"))
result = animator.generate(AnimationRequest(
    source_image_path=image,
    expression=MemeExpression.SHOCK,
    animation_backend=AnimationBackend.CARTOON_RIG,
    output_format="gif",
    fps=8,
    resolution=(256, 256),
))

print("output:", result.output_path)
print("backend:", result.backend_used)
print("frame_count:", result.frame_count)
print("metrics:", result.metrics)
PY
```

期望：

```text
backend: cartoon_rig
output: outputs/animations/xxx.gif
```

这一步不应加载 LivePortrait，也不应加载 ToonCrafter。

## 9. 启动 FastAPI 服务

终端 1：

```bash
cd ~/workspace/emoSVG
conda activate EmoSVG
set -a
source .env
set +a

uvicorn src.api.main:app --host 0.0.0.0 --port 8000 --workers 1
```

看到：

```text
Uvicorn running on http://0.0.0.0:8000
```

说明服务已启动。

## 10. API 跑通：cartoon_rig 动画

终端 2：

```bash
cd ~/workspace/emoSVG
conda activate EmoSVG
set -a
source .env
set +a
```

一行 curl：

```bash
curl -X POST http://127.0.0.1:8000/animate -F "file=@/data3/zhengmuhan/workspace/emoSVG/test_data/1.png" -F "expression=shock" -F "output_format=gif" -F "backend=cartoon_rig" -F "debug=true" -F "fps=8" -F "width=256" -F "height=256"
```

期望返回类似：

```json
{
  "output_path": "outputs/animations/xxx.gif",
  "frame_count": 21,
  "duration_ms": 2625.0,
  "backend_used": "cartoon_rig",
  "metrics": {
    "frame_count": 21.0,
    "temporal_mean_absdiff": 0.01
  },
  "debug_dir": "outputs/debug/xxx",
  "cartoon_confidence": 0.8,
  "cartoon_layer_count": 5
}
```

如果 `backend_used` 是 `cartoon_rig`，说明新后端跑通。

如果第一次请求很慢，并且日志在下载：

```text
openai/clip-vit-large-patch14
```

这是 IPExtractor 的 CLIP fallback 首次下载，不是 ToonCrafter。下载完成后后续会变快。

## 11. API 跑通：cartoon_rig + ToonCrafter 后处理

前提：

- `TOON_CRAFTER_REPO_PATH` 正确。
- `TOON_CRAFTER_MODEL_PATH/model.ckpt` 存在。
- `TOON_CRAFTER_MODEL_PATH/config.yaml` 存在。
- 单独跑过 ToonCrafter inference 或至少 import 通过。

请求：

```bash
curl -X POST http://127.0.0.1:8000/animate -F "file=@/data3/zhengmuhan/workspace/emoSVG/test_data/1.png" -F "expression=shock" -F "output_format=gif" -F "backend=cartoon_rig" -F "use_toon_crafter=true" -F "frames_between=1" -F "debug=true" -F "fps=8" -F "width=256" -F "height=256"
```

真正跑到 ToonCrafter 时，服务日志应出现类似：

```text
Running official ToonCrafter: ...
Official ToonCrafter completed ...
ToonCrafter smoothing done: ...
```

返回里的 `backend_used` 应类似：

```text
cartoon_rig+toon_crafter[rig_post]
```

如果失败但 fallback 可用，可能是：

```text
cartoon_rig+toon_crafter_fallback[rig_post]
```

如果看到：

```text
cartoon_rig+toon_crafter_failed[rig_post]
```

说明请求尝试了 ToonCrafter，但官方推理失败，最终使用未平滑帧。此时应查看服务日志中的具体 traceback。

## 12. 评估生成结果

生成 GIF 后，可以跑：

```bash
python scripts/evaluate_animation.py \
  --source test_data/1.png \
  --animation outputs/animations/你的输出.gif \
  --output outputs/metrics/你的输出.metrics.json
```

输出指标包括：

```text
frame_count
temporal_mean_absdiff
temporal_max_absdiff
temporal_jitter_proxy
source_similarity_mean
source_similarity_min
motion_energy
```

这些不是最终论文指标，但适合早期排查：

- `temporal_mean_absdiff` 接近 0：动画可能几乎没动。
- `temporal_max_absdiff` 特别高：可能某一帧跳变严重。
- `temporal_jitter_proxy` 高：可能闪烁/抖动。
- `source_similarity_min` 很低：可能身份或画面破坏严重。

## 13. 烟雾测试

普通烟雾测试：

```bash
python scripts/smoke_test.py
```

期望：

```text
Result: 5/5 passed
```

注意：

- `smoke_test.py` 仍覆盖老全管线，包括 LivePortrait/TripoSR/SAM/ToonCrafter fallback/真实路径。
- 如果只验证新 Q 版路线，优先看第 7-12 节。

## 14. 常见问题

### 14.1 `U` / untracked 文件是什么意思

IDE 里 `U` 表示 Git 未跟踪文件，通常是新创建的源码或文档：

```text
?? src/modules/cartoon_rig/
?? scripts/evaluate_animation.py
```

这不是错误。确认要提交时执行：

```bash
git add 路径
```

### 14.2 `.env` 里写了变量，但 Python 读到 None

需要每个新终端执行：

```bash
set -a
source .env
set +a
```

然后验证：

```bash
python - <<'PY'
import os
print(os.getenv("TOON_CRAFTER_REPO_PATH"))
PY
```

### 14.3 curl 报 `Failed to open/read local data from file/application`

说明 `file=@...` 后面的图片路径不存在。

检查：

```bash
ls -lh /data3/zhengmuhan/workspace/emoSVG/test_data/1.png
```

也要注意多行 curl 中反斜杠 `\` 后面不能有空格。最稳妥是直接用本文的一行命令。

### 14.4 ToonCrafter 请求很快返回，不像真的跑了

看服务日志。如果出现：

```text
ToonCrafter smoothing failed (...) -- using unsmoothed frames.
```

说明没有真正跑到官方 ToonCrafter。重点检查：

```bash
echo "$TOON_CRAFTER_REPO_PATH"
ls "$TOON_CRAFTER_REPO_PATH/scripts/evaluation/inference.py"
ls -lh "$TOON_CRAFTER_MODEL_PATH/model.ckpt" "$TOON_CRAFTER_MODEL_PATH/config.yaml"
```

### 14.5 ToonCrafter OOM

优先降低请求参数：

```text
width=256
height=256
frames_between=1
fps=8
```

当前项目中 rig 后处理会按 pair 调 ToonCrafter，显存和时间都比纯 rig 高很多。若 24G 显存仍 OOM，建议后续把 ToonCrafter 单独服务化，或只对关键 pair 做插帧。

### 14.6 LivePortrait 报 `cuda:None`

通常是 LivePortrait 官方配置里 device id 没被正确填入，项目里已经做过兼容修复。若重新出现，先确认代码是最新版本，并确认服务是重启后的进程。

### 14.7 TripoSR 报 Half/Float 类型错误

保持：

```bash
export TRIPOSR_FORCE_FLOAT32=1
```

或者确认 `.env` 里有：

```text
TRIPOSR_FORCE_FLOAT32=1
```

### 14.8 Albumentations 更新 warning

可忽略。若想关闭：

```bash
export NO_ALBUMENTATIONS_UPDATE=1
```

或写入 `.env`：

```text
NO_ALBUMENTATIONS_UPDATE=1
```

## 15. 推荐的最终验证顺序

按这个顺序走，最不容易卡：

```bash
# 1. 激活环境和变量
conda activate EmoSVG
cd ~/workspace/emoSVG
set -a
source .env
set +a

# 2. 核心 import
python - <<'PY'
import cv2, numpy, torch
from src.modules.cartoon_analyzer import CartoonFaceAnalyzer
from src.modules.cartoon_layer_parser import CartoonLayerParser
from src.modules.cartoon_rig import CartoonRigAnimator
from src.modules.cartoon_evaluator import evaluate_animation_frames
print("core + cartoon modules OK")
print("cuda:", torch.cuda.is_available())
PY

# 3. 诊断图层
python scripts/cartoon_diagnostics.py test_data/1.png --output-root outputs/debug_cartoon_1

# 4. 直接生成 cartoon_rig GIF
python - <<'PY'
from pathlib import Path
from src.modules.meme_animator.animator import MemeAnimator
from src.modules.meme_animator.schemas import AnimationRequest, AnimationBackend, MemeExpression
result = MemeAnimator(output_dir=Path("outputs/animations")).generate(AnimationRequest(
    source_image_path=Path("test_data/1.png"),
    expression=MemeExpression.SHOCK,
    animation_backend=AnimationBackend.CARTOON_RIG,
    output_format="gif",
    fps=8,
    resolution=(256, 256),
))
print(result.output_path)
print(result.backend_used)
print(result.metrics)
PY

# 5. 全量测试
python -m pytest -q
```

如果这五步都过，说明新阶段 1-5 的工程链路已经跑通。

## 16. 后续建议

当前最值得继续优化的是：

1. 用真实 Q 版/动漫关键点模型替换启发式 `CartoonFaceAnalyzer`。
2. 用 See-through/SAM-HQ/扩散补全替换当前几何图层 parser。
3. 给 `cartoon_rig` 增加 mesh warp、局部控制点、手动修正 JSON。
4. 建一个小型 Q 版表情包测试集，用 `scripts/evaluate_animation.py` + 人工评分做稳定评估。
5. ToonCrafter 保持后处理定位，不要再把它当作解决五官定位的主模块。
