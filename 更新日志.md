# emoSVG 阶段 1-5 更新日志

本文记录本轮围绕“Q 版/二次元表情包角色动画不可用”问题所完成的阶段 1-5 改造。核心方向是把项目从单纯依赖真人脸重演模型的像素驱动路线，推进到更适合 Q 版角色的“语义诊断 + 显式图层 + 2D rig + 可评估”的工作流。

> 阶段 0 主要是问题定位与技术路线判断：当前 LivePortrait + ToonCrafter 路线对 Q 版人物存在五官位置失配、真人脸先验错配、图层/遮挡关系缺失等问题。本文聚焦已经落地到代码中的阶段 1-5。

## 总体变化

本轮构建出的新系统能力可以概括为：

1. 对输入角色图进行 Q 版五官与头部几何诊断。
2. 基于几何诊断生成粗粒度可调试图层：前景、干净脸底、左眼、右眼、嘴、可选头发/手部遮挡层。
3. 输出 debug 可视化资产，帮助定位眼睛、嘴巴、mask、分层是否准确。
4. 新增 `cartoon_rig` 显式 2D 动画后端，绕开 LivePortrait 的真人脸拓扑假设。
5. 让 ToonCrafter 从“疑似主动画方案”回到更合理的位置：作为 rig 后处理插帧/平滑器。
6. 为动画输出增加轻量指标，支持之后做批量对比、回归测试和实验记录。
7. API 支持 `backend=live_portrait|cartoon_rig|auto` 和 `debug=true`，可以在同一服务内切换旧路线与新路线。

## 阶段 1：Q 版角色几何诊断

### 目标

解决“Q 版人物五官位置失配”的第一步，不再默认使用真人脸关键点。系统先用可解释、可调试的启发式方法估计：

- 角色前景区域
- 头/脸区域
- 左眼 bbox、右眼 bbox
- 嘴部 bbox
- 五官中心点
- 检测置信度与 warning

### 新增成果

新增模块：

- `src/modules/cartoon_analyzer/`
- 核心类：`CartoonFaceAnalyzer`
- 数据结构：`CartoonFaceAnalysis`、`CartoonFaceGeometry`、`BBox`、`Point2D`

该模块目前是启发式版本，重点不是追求一次到位的学术 SOTA，而是给后续系统建立稳定接口。之后可把内部替换为 StylizedFacePoint、动漫关键点检测器或手工标注微调模型，而不影响下游 API。

### 当前实现逻辑

- 通过角落背景颜色估计前景 mask。
- 根据前景 bbox 估计 Q 版头部区域。
- 在头部上半区寻找高饱和/深色眼睛候选。
- 结合左右位置、面积、纵向一致性选择眼睛对。
- 在眼睛下方区域寻找红/橙/暗色嘴部候选。
- 检测失败时退回几何 fallback，保证系统不中断。

### 价值

这一步让项目第一次拥有了“Q 版角色自己的几何坐标系”，后续眼睛/嘴巴变形可以围绕显式 bbox 和 pivot 做，而不是让真人脸隐式关键点硬套到二次元图像上。

## 阶段 2：语义图层解析与调试资产

### 目标

解决旧路线“所有像素粘在一起变形”的问题。即便还没有接入 See-through/LayerDiff 这种强图层分解模型，也先建立一套工程接口，把输入图拆成可被 rig 使用的基础图层。

### 新增成果

新增模块：

- `src/modules/cartoon_layer_parser/`
- 核心类：`CartoonLayerParser`
- 数据结构：`CartoonLayer`、`CartoonLayerParseResult`

新增调试输出：

- `src/pipeline/debug_artifacts.py`
- 核心类：`PipelineDebugWriter`

新增诊断脚本：

- `scripts/cartoon_diagnostics.py`

### 当前输出图层

当前 parser 会生成：

- `foreground`：角色整体前景。
- `face_base_clean`：通过 inpaint 去掉眼睛/嘴巴后的脸底。
- `left_eye`：左眼 RGBA 图层。
- `right_eye`：右眼 RGBA 图层。
- `mouth`：嘴巴 RGBA 图层。
- `hair_front`：低置信度头发/前景遮挡候选，当前主要用于 debug。
- `hands`：低置信度手部候选，当前主要用于 debug。

### Debug 目录内容

当 API 或 pipeline 使用 `debug=true` 时，会输出：

- `input.png`
- `landmark_overlay.png`
- `masks_overlay.png`
- `layers/*.png`
- `layers/*_mask.png`
- `keyframe_contact_sheet.jpg`
- `metrics.json`

### 使用方式

命令行诊断：

```bash
python scripts/cartoon_diagnostics.py path/to/character.png --output-root outputs/debug_cartoon
```

API 请求中开启：

```bash
curl -X POST http://127.0.0.1:8000/animate -F "file=@/data3/zhengmuhan/workspace/emoSVG/test_data/1.png" -F "expression=shock" -F "output_format=gif" -F "backend=cartoon_rig" -F "debug=true" -F "width=256" -F "height=256"
```

### 价值

这一步让系统从“黑盒失败”变成“可观察失败”。当输出不好时，可以直接看眼睛 bbox 是否偏、嘴巴 mask 是否错、脸底 inpaint 是否破坏角色，而不是只能猜 LivePortrait 为什么崩。

## 阶段 3：显式 2D Cartoon Rig 后端

### 目标

构建一条不依赖真人脸重演的 Q 版动画后端。对当前表情包人物，最小可行路线是显式控制眼睛、嘴巴和头部，而不是通过真人脸视频重演模型整体扭曲。

### 新增成果

新增模块：

- `src/modules/cartoon_rig/`
- 核心类：
  - `CartoonRigBuilder`
  - `CartoonRigAnimator`
- 数据结构：
  - `RigLayer`
  - `CartoonRigAsset`

### Rig 构建逻辑

`CartoonRigBuilder` 会把阶段 1/2 的几何与图层转换成一个可动画资产：

- `base_bgr`：擦除眼睛/嘴巴后的底图。
- `left_eye`、`right_eye`、`mouth`：独立 RGBA 控制层。
- 每个控制层有自己的 bbox、pivot、role、z-index。

### Rig 动画逻辑

`CartoonRigAnimator` 根据原有 `MotionDesigner` 产生的 `SquashParams` 渲染每帧：

- 眼睛：
  - 根据 `eye_bulge_scale` 做放大。
  - 根据 `eye_squint_scale` 做眯眼/压缩。
  - 根据 `brow_raise_offset` 做纵向位移。
- 嘴巴：
  - 根据 `mouth_width_scale` 做横向扩张。
  - 根据 `jaw_drop_scale` 做张嘴拉伸。
  - 根据 `mouth_corner_offset` 做嘴角偏移。
- 头部：
  - 根据 `head_squash_scale`、`head_stretch_scale`、`head_tilt_deg` 做轻量 squash/stretch/tilt。

### Pipeline/API 接入

新增动画后端枚举：

```text
live_portrait
cartoon_rig
auto
```

涉及文件：

- `src/modules/meme_animator/schemas.py`
- `src/modules/meme_animator/animator.py`
- `src/pipeline/full_pipeline.py`
- `src/api/routers/animate.py`
- `src/api/routers/generate.py`
- `src/api/routers/batch.py`

### 使用方式

显式指定新后端：

```bash
curl -X POST http://127.0.0.1:8000/animate -F "file=@/data3/zhengmuhan/workspace/emoSVG/test_data/1.png" -F "expression=shock" -F "output_format=gif" -F "backend=cartoon_rig" -F "fps=8" -F "width=256" -F "height=256"
```

自动路由：

```bash
curl -X POST http://127.0.0.1:8000/animate -F "file=@/data3/zhengmuhan/workspace/emoSVG/test_data/1.png" -F "expression=shock" -F "backend=auto" -F "width=256" -F "height=256"
```

当前 `auto` 的逻辑是：如果 cartoon geometry confidence 足够高，则选择 `cartoon_rig`；否则回退到 `live_portrait`。

### 价值

这是本轮最关键的系统转向：项目现在拥有了一条真正面向 Q 版图像的动画路径。它虽然还不是最终 Live2D/PSD 级别系统，但已经从“真人脸模型硬套”变为“显式五官与头部控制”。

## 阶段 4：ToonCrafter 作为 Rig 后处理插帧

### 目标

纠正 ToonCrafter 在管线中的角色定位。它不应该被理解为解决五官定位的主方法，而更适合作为关键帧之间的生成式插帧/平滑器。

### 改造成果

在 `MemeAnimator` 中重新整理了 ToonCrafter 使用逻辑：

- `live_portrait` 后端：
  - 仍支持原来的 driver/interpolator 模式。
- `cartoon_rig` 后端：
  - ToonCrafter 被作为 `rig_post` 后处理。
  - 先由显式 rig 生成关键帧。
  - 再用 ToonCrafter 或 fallback 插帧补齐中间帧。

### Backend 标识变化

输出的 `backend_used` 会明确标识实际路径，例如：

```text
cartoon_rig
cartoon_rig+toon_crafter[rig_post]
cartoon_rig+toon_crafter_fallback[rig_post]
cartoon_rig+toon_crafter_failed[rig_post]
live_portrait
live_portrait+toon_crafter[interpolator]
live_portrait+toon_crafter[driver]
```

这避免了之前“请求里 use_toon_crafter=true，但实际失败后仍像成功”的误判。

### 使用方式

rig + ToonCrafter 后处理：

```bash
curl -X POST http://127.0.0.1:8000/animate -F "file=@/data3/zhengmuhan/workspace/emoSVG/test_data/1.png" -F "expression=shock" -F "output_format=gif" -F "backend=cartoon_rig" -F "use_toon_crafter=true" -F "frames_between=1" -F "fps=8" -F "width=256" -F "height=256"
```

### 价值

这一步让 ToonCrafter 的作用更清晰：它负责“帧间平滑/生成式补帧”，不负责“理解 Q 版五官在哪里”。这也符合后续系统设计：先结构化控制，再生成式增强。

## 阶段 5：动画评估指标、测试与 API 可观测性

### 目标

建立最小可行的评估闭环。之后优化动画效果时，不能只靠肉眼主观判断，也要能在批量实验中记录基础指标。

### 新增成果

新增模块：

- `src/modules/cartoon_evaluator/`
- 核心函数：`evaluate_animation_frames`

新增评估脚本：

- `scripts/evaluate_animation.py`

### 当前指标

目前指标是轻量、无模型依赖的 proxy metrics：

- `frame_count`：帧数。
- `temporal_mean_absdiff`：相邻帧平均变化幅度。
- `temporal_max_absdiff`：最大相邻帧变化。
- `temporal_jitter_proxy`：相邻帧变化幅度的二阶波动，粗略反映抖动。
- `source_similarity_mean`：动画帧与源图的平均相似度。
- `source_similarity_min`：动画帧与源图的最低相似度。
- `motion_energy`：整体运动能量 proxy。

这些指标不能替代人工审美评价，也不能等价于 FVD/LPIPS/身份保持分数，但足够作为早期工程回归指标：比如某次改动后动画突然静止、突然剧烈闪烁、身份破坏明显，都能被初步发现。

### 使用方式

```bash
python scripts/evaluate_animation.py --source path/to/source.png --animation outputs/animations/result.gif --output outputs/metrics/result.json
```

API 返回中新增：

- `metrics`
- `debug_dir`
- `cartoon_confidence`
- `cartoon_layer_count`

适用接口：

- `POST /animate`
- `POST /generate`
- `POST /batch/generate`

### 测试覆盖

新增/扩展测试：

- `tests/unit/test_cartoon_diagnostics.py`
- `tests/integration/test_full_pipeline.py`

覆盖内容包括：

- Q 版眼睛/嘴巴几何检测。
- 图层解析输出。
- debug artifact 写出。
- rig builder 可生成可编辑部件。
- rig animator 可产生实际运动帧。
- cartoon backend 通过 `MemeAnimator` 输出 GIF。
- ToonCrafter fallback 作为 rig 后处理能增加帧数。
- `/animate` 支持 `backend=cartoon_rig` 和 `debug=true`。

### 验证结果

本轮最终验证：

```text
ruff check: passed
py_compile: passed
targeted tests: 9 passed
full tests: 262 passed
```

测试中唯一 warning 是本机 pytest 对 `asyncio_mode` 配置的提示，与本轮系统改造无关。

## 当前系统能力边界

本轮完成的是“可运行、可调试、可迭代”的工程基座，不是最终高质量动漫动画系统。当前仍有明确限制：

1. Q 版关键点检测仍是启发式，不是学习型 StylizedFacePoint。
2. 图层分解仍是几何 mask + inpaint，不是真正 See-through/PSD 级分层补全。
3. Rig 只有眼睛、嘴巴、头部的基础显式控制，还没有完整骨骼、网格蒙皮、Live2D 参数体系。
4. 头发、手、衣服等次级运动当前只作为候选层/debug 层，还没有稳定物理模拟。
5. 评估指标仍是轻量 proxy，没有接入 LPIPS/FVD/卡通身份模型。

## 对后续研发的意义

阶段 1-5 让 emoSVG 的后续优化有了清晰接口：

- 若要提升五官定位：替换 `CartoonFaceAnalyzer` 内部实现。
- 若要提升分层质量：替换 `CartoonLayerParser` 为 See-through/SAM-HQ/扩散补全方案。
- 若要提升动画质量：扩展 `CartoonRigBuilder` 和 `CartoonRigAnimator`，加入 mesh warp、ARAP、Live2D 参数。
- 若要提升插帧质量：继续优化 ToonCrafter `rig_post` 路径，或替换为更轻量的卡通插帧模型。
- 若要做实验对比：复用 `metrics`、`debug_dir`、`scripts/evaluate_animation.py` 做批量记录。

换句话说，本轮不是简单加了几个 fallback，而是把项目从“模型黑盒调用管线”改造成了一个可以逐步替换核心算法的结构化动漫角色动画原型系统。
