# emoSVG 环境搭建指南

本文档记录截至当前已经跑通的服务器环境搭建流程，目标是在课题组 Linux GPU 服务器上完整跑通：

```text
IPExtractor -> LivePortrait 动画 -> TripoSR 3D -> SAM/SVG -> ToonCrafter 补帧
```

已验证服务器路径示例：

```text
项目目录：/data3/zhengmuhan/workspace/emoSVG
第三方源码：/data3/zhengmuhan/workspace/third_party
模型权重：/data3/zhengmuhan/workspace/emosvg_models
输出目录：/data3/zhengmuhan/workspace/emosvg_outputs
conda 环境：EmoSVG
GPU：24 GB 4090
```

## 1. 总体原则

不要一上来安装第三方项目的完整 requirements。LivePortrait、ToonCrafter、TripoSR 都是研究项目，完整 requirements 很容易改坏主环境里的 `torch`、`numpy`、`opencv`、`transformers`。

推荐顺序：

1. 新建干净 conda 环境。
2. 安装 emoSVG 主依赖。
3. 固定 NumPy / OpenCV / PyTorch 版本。
4. 第三方源码只挂路径，缺小包再单独补。
5. 下载权重。
6. smoke test。
7. API 测 LivePortrait。
8. 单独测官方 ToonCrafter。
9. 再打开 `/animate use_toon_crafter=true`。

## 2. 新建环境

```bash
conda deactivate
conda create -n EmoSVG python=3.10 -y
conda activate EmoSVG

cd ~/workspace/emoSVG
python -m pip install --upgrade pip setuptools wheel
```

## 3. 安装 PyTorch

CUDA 12.1 推荐：

```bash
pip install torch==2.3.0 torchvision==0.18.0 --index-url https://download.pytorch.org/whl/cu121
```

CUDA 11.8 可用：

```bash
pip install torch==2.3.0 torchvision==0.18.0 --index-url https://download.pytorch.org/whl/cu118
```

验证：

```bash
python - <<'PY'
import torch, torchvision
print("torch:", torch.__version__)
print("torchvision:", torchvision.__version__)
print("cuda available:", torch.cuda.is_available())
print("torch cuda:", torch.version.cuda)
if torch.cuda.is_available():
    print("gpu:", torch.cuda.get_device_name(0))
PY
```

期望：

```text
cuda available: True
```

## 4. 安装 emoSVG 主依赖

```bash
cd ~/workspace/emoSVG
pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu121
pip install -e .
```

如果下载慢，普通 Python 包可以使用清华源：

```bash
-i https://pypi.tuna.tsinghua.edu.cn/simple
```

## 5. 固定 NumPy 和 OpenCV

推荐组合：

```bash
pip install "numpy==1.26.4" -i https://pypi.tuna.tsinghua.edu.cn/simple
pip uninstall opencv-contrib-python opencv-python opencv-python-headless -y
pip install opencv-python-headless==4.9.0.80 -i https://pypi.tuna.tsinghua.edu.cn/simple
```

验证：

```bash
python - <<'PY'
import numpy as np
import cv2
print("numpy:", np.__version__)
print("cv2:", cv2.__version__)
PY
```

期望：

```text
numpy: 1.26.4
cv2: 4.9.0
```

## 6. 准备第三方源码

推荐目录：

```text
~/workspace/third_party/segment-anything
~/workspace/third_party/TripoSR
~/workspace/third_party/LivePortrait
~/workspace/third_party/ToonCrafter
```

如果 GitHub clone 慢或失败，可以本地下载 zip 后用 `scp` 上传到服务器，再解压到 `~/workspace/third_party`。

## 7. 挂第三方源码路径

不要全量安装 `LivePortrait/requirements.txt` 或 `ToonCrafter/requirements.txt`。

```bash
SITE_PACKAGES=$(python - <<'PY'
import site
print(site.getsitepackages()[0])
PY
)

echo "$HOME/workspace/third_party/TripoSR" > "$SITE_PACKAGES/emosvg_triposr.pth"
echo "$HOME/workspace/third_party/ToonCrafter" > "$SITE_PACKAGES/emosvg_tooncrafter.pth"
```

如果目录是 `*-main`，就改成实际目录：

```bash
echo "$HOME/workspace/third_party/TripoSR-main" > "$SITE_PACKAGES/emosvg_triposr.pth"
echo "$HOME/workspace/third_party/ToonCrafter-main" > "$SITE_PACKAGES/emosvg_tooncrafter.pth"
```

`segment-anything` 可以直接安装源码包：

```bash
pip install ~/workspace/third_party/segment-anything
```

## 8. LivePortrait 兼容包壳

LivePortrait 官方 zip 常见结构是：

```text
LivePortrait/src/live_portrait_pipeline.py
LivePortrait/src/config/inference_config.py
```

它内部使用相对导入，所以不要直接把 `LivePortrait/src` 当成顶层模块导入。创建兼容包：

```bash
mkdir -p ~/workspace/third_party/liveportrait_compat
cp -a ~/workspace/third_party/LivePortrait/src \
  ~/workspace/third_party/liveportrait_compat/liveportrait
touch ~/workspace/third_party/liveportrait_compat/liveportrait/__init__.py

echo "$HOME/workspace/third_party/liveportrait_compat" > "$SITE_PACKAGES/emosvg_liveportrait.pth"
```

补 LivePortrait 小依赖：

```bash
pip install tyro pykalman lmdb ffmpeg-python -i https://pypi.tuna.tsinghua.edu.cn/simple
```

## 9. ToonCrafter 官方脚本小依赖

只补缺失小包，不装完整 requirements。

```bash
pip install --no-deps pytorch-lightning==1.9.3 torchmetrics==0.11.4 lightning-utilities==0.15.2 \
  -i https://pypi.tuna.tsinghua.edu.cn/simple

pip install setuptools==69.5.1 -i https://pypi.tuna.tsinghua.edu.cn/simple

pip install --no-deps open_clip_torch==2.22.0 -i https://pypi.tuna.tsinghua.edu.cn/simple
pip install --no-deps ftfy sentencepiece "protobuf>=4.25.3,<5" -i https://pypi.tuna.tsinghua.edu.cn/simple

pip install --no-deps xformers==0.0.26.post1 -i https://pypi.tuna.tsinghua.edu.cn/simple
pip install av==12.3.0 -i https://pypi.tuna.tsinghua.edu.cn/simple
```

验证 `xformers`：

```bash
python - <<'PY'
import torch
import xformers
import xformers.ops
print("torch:", torch.__version__, torch.version.cuda)
print("xformers:", xformers.__version__)
q = torch.randn(1, 64, 8, 40, device="cuda", dtype=torch.float16)
out = xformers.ops.memory_efficient_attention(q, q, q)
print("xformers attention OK:", out.shape)
PY
```

## 10. 配置 `.env`

在项目根目录：

```bash
cd ~/workspace/emoSVG
cp .env.example .env
```

建议 `.env` 内容至少包括：

```bash
DEVICE=cuda
TORCH_DTYPE=float16
VRAM_BUDGET_GB=10.0

MODELS_ROOT=/data3/zhengmuhan/workspace/emosvg_models
IP_ADAPTER_MODEL_PATH=/data3/zhengmuhan/workspace/emosvg_models/ip_adapter
LIVE_PORTRAIT_MODEL_PATH=/data3/zhengmuhan/workspace/emosvg_models/live_portrait
TRIPOSR_MODEL_PATH=/data3/zhengmuhan/workspace/emosvg_models/triposr
TRIPOSR_FORCE_FLOAT32=1
TOON_CRAFTER_MODEL_PATH=/data3/zhengmuhan/workspace/emosvg_models/toon_crafter
TOON_CRAFTER_REPO_PATH=/data3/zhengmuhan/workspace/third_party/ToonCrafter
TOON_CRAFTER_MAX_SIDE=256
TOON_CRAFTER_VIDEO_LENGTH=8
TOON_CRAFTER_DDIM_STEPS=10
TOON_CRAFTER_PERFRAME_AE=1
TOON_CRAFTER_TIMEOUT_SEC=1800
SAM_MODEL_PATH=/data3/zhengmuhan/workspace/emosvg_models/sam/sam_vit_h_4b8939.pth

HF_ENDPOINT=https://hf-mirror.com
HF_HOME=/data3/zhengmuhan/workspace/emosvg_models/.hf_cache

OUTPUT_ROOT=/data3/zhengmuhan/workspace/emosvg_outputs
API_HOST=0.0.0.0
API_PORT=8000
API_WORKERS=1

PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True
NO_ALBUMENTATIONS_UPDATE=1
```

注意 `.env` 不能写成等号两边有空格：

```bash
# 错误
TOON_CRAFTER_REPO_PATH = /data3/...

# 正确
TOON_CRAFTER_REPO_PATH=/data3/...
```

每个新终端都要加载：

```bash
set -a
source .env
set +a
```

`set -a` 会让 `.env` 里的变量自动 export，这样 Python、uvicorn、子进程都能读到。

## 11. 验证 import

```bash
python - <<'PY'
checks = []

try:
    import segment_anything
    checks.append(("segment_anything", "OK"))
except Exception as e:
    checks.append(("segment_anything", repr(e)))

try:
    import tsr
    checks.append(("TripoSR tsr", "OK"))
except Exception as e:
    checks.append(("TripoSR tsr", repr(e)))

try:
    from liveportrait.live_portrait_pipeline import LivePortraitPipeline
    checks.append(("LivePortrait", "OK"))
except Exception as e:
    checks.append(("LivePortrait", repr(e)))

try:
    import lvdm
    checks.append(("ToonCrafter lvdm", "OK"))
except Exception as e:
    checks.append(("ToonCrafter lvdm", repr(e)))

for name, result in checks:
    print(f"{name}: {result}")
PY
```

期望：

```text
segment_anything: OK
TripoSR tsr: OK
LivePortrait: OK
ToonCrafter lvdm: OK
```

## 12. 下载模型权重

先加载 `.env`：

```bash
cd ~/workspace/emoSVG
set -a
source .env
set +a
```

### 12.1 TripoSR

```bash
python scripts/download_models.py --models triposr
```

验收：

```bash
ls -lh "$TRIPOSR_MODEL_PATH/config.yaml"
ls -lh "$TRIPOSR_MODEL_PATH/model.ckpt"
```

### 12.2 LivePortrait

推荐用 HF mirror：

```bash
HF_ENDPOINT=https://hf-mirror.com huggingface-cli download KwaiVGI/LivePortrait \
  --repo-type model \
  --local-dir "$LIVE_PORTRAIT_MODEL_PATH" \
  --resume-download
```

验收：

```bash
find "$LIVE_PORTRAIT_MODEL_PATH" -maxdepth 6 -type f | sort | grep -E "appearance|motion|warping|spade|stitching|retargeting"
```

### 12.3 SAM

```bash
mkdir -p "$MODELS_ROOT/sam"
cd "$MODELS_ROOT/sam"
wget -c https://dl.fbaipublicfiles.com/segment_anything/sam_vit_h_4b8939.pth
```

验收：

```bash
ls -lh "$SAM_MODEL_PATH"
```

### 12.4 ToonCrafter

```bash
cd ~/workspace/emoSVG
python scripts/download_models.py --models toon_crafter
```

验收：

```bash
ls -lh "$TOON_CRAFTER_MODEL_PATH/model.ckpt"
ls -lh "$TOON_CRAFTER_MODEL_PATH/config.yaml"
ls -lh "$TOON_CRAFTER_MODEL_PATH/configs/inference_512_v1.0.yaml"
ls -lh "$TOON_CRAFTER_MODEL_PATH/checkpoints/tooncrafter_512_interp_v1/model.ckpt"
```

### 12.5 IP-Adapter 可选

如果不下载，项目会使用 HuggingFace CLIP fallback；首次调用可能临时下载 1.71GB 左右的 CLIP。

```bash
python scripts/download_models.py --models ip_adapter
```

## 13. smoke test

```bash
cd ~/workspace/emoSVG
set -a
source .env
set +a

python scripts/smoke_test.py
```

期望：

```text
Result: 5/5 passed
```

注意：smoke test 通过说明主链路能跑，不代表 ToonCrafter 一定已经真实启用。

## 14. 单独验证官方 ToonCrafter

准备测试帧：

```bash
mkdir -p /data3/zhengmuhan/emosvg_test/tooncrafter_pair
cp /data3/zhengmuhan/workspace/emoSVG/test_data/1.png /data3/zhengmuhan/emosvg_test/tooncrafter_pair/0001.png
cp /data3/zhengmuhan/workspace/emoSVG/test_data/1.png /data3/zhengmuhan/emosvg_test/tooncrafter_pair/0002.png
echo "a cartoon character animation" > /data3/zhengmuhan/emosvg_test/tooncrafter_pair/prompts.txt
```

进入官方源码目录：

```bash
conda activate EmoSVG
cd ~/workspace/third_party/ToonCrafter
set -a
source ~/workspace/emoSVG/.env
set +a
```

先看 help：

```bash
python scripts/evaluation/inference.py --help | head -80
```

小尺寸推理：

```bash
python scripts/evaluation/inference.py \
  --ckpt_path "$TOON_CRAFTER_MODEL_PATH/model.ckpt" \
  --config "$TOON_CRAFTER_MODEL_PATH/config.yaml" \
  --prompt_dir /data3/zhengmuhan/emosvg_test/tooncrafter_pair \
  --savedir /data3/zhengmuhan/emosvg_test/tooncrafter_out \
  --height 256 \
  --width 256 \
  --video_length 16 \
  --interp \
  --ddim_steps 25 \
  --bs 1 \
  --perframe_ae
```

成功标志：

```text
>>> model checkpoint loaded.
Saved in /data3/zhengmuhan/emosvg_test/tooncrafter_out
```

查看输出：

```bash
find /data3/zhengmuhan/emosvg_test/tooncrafter_out -type f | sort | tail -20
```

## 15. 启动 API

```bash
conda activate EmoSVG
cd ~/workspace/emoSVG

set -a
source .env
set +a

uvicorn src.api.main:app --host 0.0.0.0 --port 8000 --workers 1
```

如果端口占用，换端口：

```bash
uvicorn src.api.main:app --host 0.0.0.0 --port 8010 --workers 1
```

## 16. API 测试

另开终端：

```bash
conda activate EmoSVG
cd ~/workspace/emoSVG
set -a
source .env
set +a
```

健康检查：

```bash
curl http://127.0.0.1:8000/health
curl http://127.0.0.1:8000/status
```

不启用 ToonCrafter：

```bash
curl -X POST http://127.0.0.1:8000/animate -F "file=@/data3/zhengmuhan/workspace/emoSVG/test_data/1.png" -F "expression=shock" -F "output_format=gif" -F "use_toon_crafter=false" -F "fps=12" -F "width=512" -F "height=512"
```

启用 ToonCrafter，保守参数：

```bash
curl -X POST http://127.0.0.1:8000/animate -F "file=@/data3/zhengmuhan/workspace/emoSVG/test_data/1.png" -F "expression=shock" -F "output_format=gif" -F "use_toon_crafter=true" -F "frames_between=1" -F "fps=8" -F "width=256" -F "height=256"
```

成功日志：

```text
Running official ToonCrafter: 20 pair(s), 256x256, 8 frames, 10 DDIM steps.
>>> model checkpoint loaded.
Prompts testing [rank:0] 20/20 samples loaded.
Saved in ...
Official ToonCrafter completed ...
ToonCrafter smoothing done: 41 total frames
```

成功返回的输出文件类似：

```text
outputs/animations/tmp0v5s3aev_shock_1778742767.gif
```

## 17. 结果查看

```bash
ls -lh outputs/animations/*.gif | tail
```

如果需要本地查看，可以从本地机器拉取：

```bash
scp user@server:/data3/zhengmuhan/workspace/emoSVG/outputs/animations/xxx.gif .
```

## 18. 常见判断

### 18.1 `curl: (26) Failed to open/read local data`

图片路径不存在。先确认：

```bash
ls -lh /data3/zhengmuhan/workspace/emoSVG/test_data/1.png
```

### 18.2 `TOON_CRAFTER_REPO_PATH=None`

说明 `.env` 没加载或写法错。验证：

```bash
python - <<'PY'
import os
print(repr(os.getenv("TOON_CRAFTER_REPO_PATH")))
PY
```

### 18.3 API 返回 200 但 ToonCrafter 没生效

看服务端日志。如果有：

```text
ToonCrafter smoothing failed
using unsmoothed frames
```

说明回退了。真实成功必须看到：

```text
Running official ToonCrafter
>>> model checkpoint loaded.
ToonCrafter smoothing done
```

### 18.4 ToonCrafter OOM

先使用：

```bash
TOON_CRAFTER_MAX_SIDE=256
TOON_CRAFTER_VIDEO_LENGTH=8
TOON_CRAFTER_DDIM_STEPS=10
```

再逐步提高：

```bash
TOON_CRAFTER_MAX_SIDE=384
TOON_CRAFTER_VIDEO_LENGTH=16
TOON_CRAFTER_DDIM_STEPS=25
```

每次只调一个参数。

## 19. 当前已跑通状态

截至当前，已完成：

```text
主环境 import：OK
smoke test：5/5 passed
LivePortrait API 动画：OK
TripoSR 本地权重：OK
SAM 权重：OK
ToonCrafter 权重：OK
ToonCrafter 官方脚本：OK
/animate use_toon_crafter=true：OK
```

已验证的 ToonCrafter API 成功形态：

```text
21 keyframes -> 41 total frames
backend_used=live_portrait+toon_crafter[interpolator]
```
