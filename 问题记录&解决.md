# emoSVG 问题记录 & 解决方法

本文记录本次在课题组服务器跑通 emoSVG 时遇到的问题、原因判断和可行解决方法。后续如果重新配环境或换服务器，建议按本文顺序排查。

## 0. 当前已知状态

截至本次记录：

| 模块 | 状态 | 说明 |
|---|---|---|
| conda 环境 | 已重建为 `EmoSVG` | 不建议继续使用被第三方 requirements 污染过的旧环境 |
| 项目主依赖 | 基本 OK | `pip install -e .` 已成功 |
| 第三方源码 import | 基本 OK | `segment_anything`、`tsr`、`liveportrait`、`lvdm` 都曾验证 OK |
| TripoSR | 权重 OK，推理问题已修过 | `config.yaml` 和 `model.ckpt` 已存在 |
| LivePortrait | 权重已下载 | HuggingFace 下载布局为 `liveportrait/...`，需同步新版 wrapper 或建软链接 |
| SAM | 正在/需要下载 | 用 `wget -c` 断点续传下载 `sam_vit_h_4b8939.pth` |
| ToonCrafter | 建议最后处理 | 权重大、依赖复杂，先不要阻塞主链路 |

推荐后续顺序：

```bash
TripoSR -> LivePortrait -> SAM -> API 真实图片 -> ToonCrafter
```

## 1. `pip install -e` 报错

现象：

```text
-e option requires 1 argument
```

原因：`-e` 后面必须跟本地项目路径。

解决：

```bash
pip install -e .
```

## 2. `ModuleNotFoundError: No module named 'cv2'`

现象：

```text
ModuleNotFoundError: No module named 'cv2'
```

原因：OpenCV 没装，或者装到了别的 conda 环境。

解决：

```bash
pip install opencv-python-headless==4.9.0.80 -i https://pypi.tuna.tsinghua.edu.cn/simple
```

验证：

```bash
python - <<'PY'
import cv2
print(cv2.__version__)
PY
```

## 3. NumPy 2.x 导致 `ptp` 报错

现象：

```text
`ptp` was removed from the ndarray class in NumPy 2.0. Use np.ptp(arr, ...) instead.
```

原因：当前项目和部分 3D / OpenCV 依赖对 NumPy 2.x 兼容不好。

解决：降级到 NumPy 1.26.4。

```bash
pip install "numpy==1.26.4" -i https://pypi.tuna.tsinghua.edu.cn/simple
```

如果之后出现：

```text
opencv-contrib-python requires numpy>=2
```

说明装了不合适的 OpenCV contrib 版本。清掉并固定 headless：

```bash
pip uninstall opencv-contrib-python opencv-python opencv-python-headless -y
pip install opencv-python-headless==4.9.0.80 -i https://pypi.tuna.tsinghua.edu.cn/simple
```

验证：

```bash
python - <<'PY'
import numpy, cv2
print("numpy:", numpy.__version__)
print("cv2:", cv2.__version__)
PY
```

期望：

```text
numpy: 1.26.4
cv2: 4.9.0
```

## 4. 不要全量安装第三方 requirements

现象：安装 ToonCrafter / LivePortrait requirements 后，pip 会回溯并改掉一堆核心包：

```text
torch
torchvision
numpy
opencv
transformers
protobuf
Pillow
imageio
xformers
gradio
```

风险：主环境被污染，可能把 `torch 2.3.0` 改成别的版本，或者把 `numpy 1.26.4` 改掉。

推荐策略：

1. 第三方源码只挂路径。
2. 缺哪个小包再单独装。
3. 不直接 `pip install -r third_party/xxx/requirements.txt`。

可单独补的小包示例：

```bash
pip install tyro pykalman lmdb ffmpeg-python -i https://pypi.tuna.tsinghua.edu.cn/simple
pip install decord omegaconf pytorch-lightning open_clip_torch timm av moviepy -i https://pypi.tuna.tsinghua.edu.cn/simple
```

如果环境已经被污染，最稳是重建 clean env。

## 5. clean conda 环境建议

创建环境：

```bash
conda create -n EmoSVG python=3.10 -y
conda activate EmoSVG
```

安装 PyTorch，按服务器 CUDA 版本选择。4090 常见可用 CUDA 12.1：

```bash
pip install torch==2.3.0 torchvision==0.18.0 --index-url https://download.pytorch.org/whl/cu121
```

安装主项目：

```bash
cd ~/workspace/emoSVG
pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu121
pip install -e .
```

再固定 NumPy 和 OpenCV：

```bash
pip install "numpy==1.26.4" -i https://pypi.tuna.tsinghua.edu.cn/simple
pip uninstall opencv-contrib-python opencv-python opencv-python-headless -y
pip install opencv-python-headless==4.9.0.80 -i https://pypi.tuna.tsinghua.edu.cn/simple
```

## 6. `.pth` 挂第三方源码路径

目的：让 Python 能 import 第三方源码，但不安装其 requirements。

```bash
SITE_PACKAGES=$(python - <<'PY'
import site
print(site.getsitepackages()[0])
PY
)

echo "$HOME/workspace/third_party/TripoSR" > "$SITE_PACKAGES/emosvg_triposr.pth"
echo "$HOME/workspace/third_party/ToonCrafter" > "$SITE_PACKAGES/emosvg_tooncrafter.pth"
```

如果目录带 `-main`：

```bash
echo "$HOME/workspace/third_party/TripoSR-main" > "$SITE_PACKAGES/emosvg_triposr.pth"
echo "$HOME/workspace/third_party/ToonCrafter-main" > "$SITE_PACKAGES/emosvg_tooncrafter.pth"
```

## 7. LivePortrait 官方源码导入问题

现象：

```text
ModuleNotFoundError: No module named 'liveportrait'
```

或：

```text
ImportError: attempted relative import with no known parent package
```

原因：LivePortrait 官方 zip 常见结构是：

```text
LivePortrait/src/live_portrait_pipeline.py
LivePortrait/src/config/inference_config.py
```

源码内部使用相对导入，不能直接把 `LivePortrait/src` 当顶层模块导入。

解决：创建兼容包壳。

```bash
mkdir -p ~/workspace/third_party/liveportrait_compat

cp -a ~/workspace/third_party/LivePortrait/src \
  ~/workspace/third_party/liveportrait_compat/liveportrait

touch ~/workspace/third_party/liveportrait_compat/liveportrait/__init__.py

SITE_PACKAGES=$(python - <<'PY'
import site
print(site.getsitepackages()[0])
PY
)

echo "$HOME/workspace/third_party/liveportrait_compat" > "$SITE_PACKAGES/emosvg_liveportrait.pth"
```

如果报缺小包：

```text
No module named 'tyro'
```

补：

```bash
pip install tyro pykalman lmdb ffmpeg-python -i https://pypi.tuna.tsinghua.edu.cn/simple
```

验证：

```bash
python - <<'PY'
from liveportrait.live_portrait_pipeline import LivePortraitPipeline
from liveportrait.config.inference_config import InferenceConfig
print("LivePortrait compat package OK")
PY
```

## 8. 第三方 import 验证命令

```bash
python - <<'PY'
checks = []

try:
    import segment_anything
    checks.append(("segment_anything", "OK"))
except Exception as e:
    checks.append(("segment_anything", repr(e)))

try:
    import tsr
    checks.append(("TripoSR tsr", "OK"))
except Exception as e:
    checks.append(("TripoSR tsr", repr(e)))

try:
    from liveportrait.live_portrait_pipeline import LivePortraitPipeline
    checks.append(("LivePortrait", "OK"))
except Exception as e:
    checks.append(("LivePortrait", repr(e)))

try:
    import lvdm
    checks.append(("ToonCrafter lvdm", "OK"))
except Exception as e:
    checks.append(("ToonCrafter lvdm", repr(e)))

for name, result in checks:
    print(f"{name}: {result}")
PY
```

期望：

```text
segment_anything: OK
TripoSR tsr: OK
LivePortrait: OK
ToonCrafter lvdm: OK
```

## 9. `.env` 没生效

现象：

```bash
ls -lh $SAM_MODEL_PATH
```

结果列出了项目根目录，而不是 SAM 文件。

原因：`$SAM_MODEL_PATH` 为空，说明当前 shell 没有加载 `.env`，或 `.env` 写法不对。

正确加载：

```bash
cd ~/workspace/emoSVG
set -a
source .env
set +a
```

检查：

```bash
env | grep -E "MODELS_ROOT|LIVE_PORTRAIT|TRIPOSR|TOON|SAM|OUTPUT_ROOT|HF_ENDPOINT"
```

`.env` 至少应包含：

```bash
MODELS_ROOT=/data3/zhengmuhan/workspace/emosvg_models
LIVE_PORTRAIT_MODEL_PATH=/data3/zhengmuhan/workspace/emosvg_models/live_portrait
TRIPOSR_MODEL_PATH=/data3/zhengmuhan/workspace/emosvg_models/triposr
TOON_CRAFTER_MODEL_PATH=/data3/zhengmuhan/workspace/emosvg_models/toon_crafter
SAM_MODEL_PATH=/data3/zhengmuhan/workspace/emosvg_models/sam/sam_vit_h_4b8939.pth
TRIPOSR_FORCE_FLOAT32=1
OUTPUT_ROOT=/data3/zhengmuhan/workspace/emosvg_outputs
HF_ENDPOINT=https://hf-mirror.com
HF_HOME=/data3/zhengmuhan/workspace/emosvg_models/.hf_cache
```

注意：shell 格式不能写成：

```bash
HF_ENDPOINT = https://hf-mirror.com
HF_ENDPOINT: https://hf-mirror.com
```

必须是：

```bash
HF_ENDPOINT=https://hf-mirror.com
```

## 10. `Done` 不等于权重下载成功

现象：脚本最后输出：

```text
Done. Model weights are in: /data3/zhengmuhan/workspace/emosvg_models
```

但后面检查发现文件不存在。

原因：`Done` 只是脚本收尾日志，不代表所有下载都成功。部分下载函数会记录 error 后继续执行。

判断是否成功，必须看文件是否存在：

```bash
ls -lh "$SAM_MODEL_PATH"
find "$LIVE_PORTRAIT_MODEL_PATH" -maxdepth 6 -type f | sort | grep -E "appearance|motion|warping|spade|stitching|retargeting"
ls -lh "$TRIPOSR_MODEL_PATH/config.yaml" "$TRIPOSR_MODEL_PATH/model.ckpt"
ls -lh "$TOON_CRAFTER_MODEL_PATH/config.yaml" "$TOON_CRAFTER_MODEL_PATH/model.ckpt"
```

## 11. TripoSR 权重与路径

成功状态：

```text
$TRIPOSR_MODEL_PATH/config.yaml
$TRIPOSR_MODEL_PATH/model.ckpt
```

本次服务器上已确认：

```text
/data3/zhengmuhan/workspace/emosvg_models/triposr/config.yaml
/data3/zhengmuhan/workspace/emosvg_models/triposr/model.ckpt
```

如果 `.env` 写成：

```bash
TRIPOSR_MODEL_PATH=./models/triposr
```

但真实权重在：

```text
/data3/zhengmuhan/workspace/emosvg_models/triposr
```

就会导致程序找不到本地权重，转而访问 HuggingFace。

应改为绝对路径：

```bash
TRIPOSR_MODEL_PATH=/data3/zhengmuhan/workspace/emosvg_models/triposr
```

## 12. TripoSR 缺 `omegaconf` / `torchmcubes`

现象：

```text
Failed to load TripoSR ... No module named 'omegaconf'
```

解决：

```bash
pip install omegaconf -i https://pypi.tuna.tsinghua.edu.cn/simple
```

现象：

```text
No module named 'torchmcubes'
```

`torchmcubes` 不是普通 PyPI 包，清华源找不到是正常的。可通过 TripoSR 官方依赖、源码或项目提供的环境方式处理。若当前 TripoSR 已可进入推理，则不要反复折腾它。

## 13. TripoSR `has_vertex_color` 签名问题

现象：

```text
TSR.extract_mesh() missing 1 required positional argument: 'has_vertex_color'
```

原因：不同版本 TripoSR 的 `extract_mesh` API 不同。

解决：同步新版 `src/modules/reconstructor_3d/reconstructor.py`。新版 wrapper 已兼容：

```python
extract_mesh(..., has_vertex_color=False)
```

以及旧签名。

## 14. TripoSR Half/Float dtype 问题

现象：

```text
TripoSR inference failed: expected scalar type Half but found Float
```

原因：全局 `TORCH_DTYPE=float16` 把模型放到 half，但 TripoSR 预处理输入是 float32。

解决：同步新版 `src/modules/reconstructor_3d/reconstructor.py`，并在 `.env` 中保留：

```bash
TRIPOSR_FORCE_FLOAT32=1
```

这会让 TripoSR 默认使用 float32，更适合 24GB 4090 稳定跑通。

## 15. TripoSR 本地目录仍触发 HuggingFace Hub

现象：

```text
Failed to load TripoSR from '/data3/.../emosvg_models/triposr':
An error happened while trying to locate the file on the Hub
```

但本地文件明明存在：

```bash
ls -lh "$TRIPOSR_MODEL_PATH/config.yaml" "$TRIPOSR_MODEL_PATH/model.ckpt"
```

原因：部分 TripoSR fork 的 `TSR.from_pretrained(local_path, ...)` 对本地目录支持不完整，仍然会走 `hf_hub_download`。

解决：同步新版：

```text
src/modules/reconstructor_3d/reconstructor.py
```

新版逻辑：

1. 如果本地目录存在 `config.yaml` 和 `model.ckpt`：

```python
cfg = OmegaConf.load(config.yaml)
model = TSR(cfg)
model.load_state_dict(torch.load(model.ckpt, map_location="cpu"))
```

2. 只有不是本地权重目录时，才调用：

```python
TSR.from_pretrained(...)
```

验证：

```bash
grep -n "_load_triposr_model\\|OmegaConf\\|from_pretrained" src/modules/reconstructor_3d/reconstructor.py
python scripts/smoke_test.py
```

## 16. smoke test 通过不代表所有真模型都启用

现象：

```text
LivePortrait weights not found at 'None'. Using affine-warp fallback.
ToonCrafter weights not found at 'None' — using linear-blend fallback.
SAM weights not found — using contour-detection fallback.
```

含义：smoke test 通过的是管线结构，不是全部真实模型质量。

如果出现 `at 'None'`，说明 smoke test 没拿到路径。需同步新版：

```text
scripts/smoke_test.py
configs/base.yaml
.env.example
```

新版 smoke test 会读取：

```bash
LIVE_PORTRAIT_MODEL_PATH
TOON_CRAFTER_MODEL_PATH
SAM_MODEL_PATH
TRIPOSR_MODEL_PATH
```

## 17. LivePortrait 权重下载和布局

推荐用 HuggingFace 镜像：

```bash
export HF_ENDPOINT=https://hf-mirror.com

huggingface-cli download KwaiVGI/LivePortrait \
  --repo-type model \
  --local-dir "$LIVE_PORTRAIT_MODEL_PATH" \
  --resume-download
```

成功后可能是这种布局：

```text
$LIVE_PORTRAIT_MODEL_PATH/liveportrait/base_models/appearance_feature_extractor.pth
$LIVE_PORTRAIT_MODEL_PATH/liveportrait/base_models/motion_extractor.pth
$LIVE_PORTRAIT_MODEL_PATH/liveportrait/base_models/warping_module.pth
$LIVE_PORTRAIT_MODEL_PATH/liveportrait/base_models/spade_generator.pth
$LIVE_PORTRAIT_MODEL_PATH/liveportrait/retargeting_models/stitching_retargeting_module.pth
```

旧 wrapper 可能只认：

```text
$LIVE_PORTRAIT_MODEL_PATH/pretrained_weights/liveportrait/...
```

解决方案 A：同步新版 `src/modules/meme_animator/live_portrait.py`。新版兼容两种布局：

```text
$LIVE_PORTRAIT_MODEL_PATH/liveportrait/...
$LIVE_PORTRAIT_MODEL_PATH/pretrained_weights/liveportrait/...
```

解决方案 B：临时软链接兼容旧代码：

```bash
mkdir -p "$LIVE_PORTRAIT_MODEL_PATH/pretrained_weights"

ln -sfn "$LIVE_PORTRAIT_MODEL_PATH/liveportrait" \
  "$LIVE_PORTRAIT_MODEL_PATH/pretrained_weights/liveportrait"

ln -sfn "$HOME/workspace/third_party/LivePortrait/src" \
  "$LIVE_PORTRAIT_MODEL_PATH/src"
```

验证：

```bash
find "$LIVE_PORTRAIT_MODEL_PATH" -maxdepth 6 -type f | sort | grep -E "appearance|motion|warping|spade|stitching|retargeting"
```

## 18. HuggingFace endpoint 没生效

现象：

```text
HF_ENDPOINT = None
```

原因：`.env` 没有正确写入 `HF_ENDPOINT=...`，或者只改了 `.env.example`。

检查：

```bash
grep -n "HF_ENDPOINT" .env .env.example
```

追加：

```bash
cat >> .env <<'EOF'

HF_ENDPOINT=https://hf-mirror.com
HF_HOME=/data3/zhengmuhan/workspace/emosvg_models/.hf_cache
EOF
```

重新加载：

```bash
set -a
source .env
set +a
```

验证：

```bash
python - <<'PY'
import os
print("HF_ENDPOINT =", os.getenv("HF_ENDPOINT"))
print("HF_HOME =", os.getenv("HF_HOME"))
PY
```

## 19. HuggingFace 下载报 `LocalEntryNotFoundError`

现象：

```text
LocalEntryNotFoundError: An error happened while trying to locate the files on the Hub
```

原因：没有连上 HuggingFace / 镜像，本地缓存也没有对应文件。

先用小文件测试：

```bash
HF_ENDPOINT=https://hf-mirror.com huggingface-cli download KwaiVGI/LivePortrait \
  README.md \
  --repo-type model \
  --local-dir /tmp/lp_test
```

如果小文件成功，再下完整模型：

```bash
HF_ENDPOINT=https://hf-mirror.com huggingface-cli download KwaiVGI/LivePortrait \
  --repo-type model \
  --local-dir "$LIVE_PORTRAIT_MODEL_PATH" \
  --resume-download
```

## 20. SAM 下载失败 / SSL EOF

现象：

```text
ssl.SSLEOFError: [SSL: UNEXPECTED_EOF_WHILE_READING]
```

原因：服务器到 `dl.fbaipublicfiles.com` 网络不稳定。SAM 不走 HuggingFace，换 `HF_ENDPOINT` 对 SAM 无效。

解决：用 `wget -c` 断点续传。

```bash
mkdir -p "$MODELS_ROOT/sam"
cd "$MODELS_ROOT/sam"
wget -c https://dl.fbaipublicfiles.com/segment_anything/sam_vit_h_4b8939.pth
```

如果没有 `wget`：

```bash
curl -L --retry 20 --retry-delay 5 -C - \
  -o "$SAM_MODEL_PATH" \
  https://dl.fbaipublicfiles.com/segment_anything/sam_vit_h_4b8939.pth
```

正常下载日志类似：

```text
HTTP request sent, awaiting response... 200 OK
Length: 2564550879 (2.4G)
Saving to: 'sam_vit_h_4b8939.pth'
```

下载完成后：

```bash
ls -lh "$SAM_MODEL_PATH"
```

期望约 2.4G。

## 21. InsightFace 自动下载 buffalo_l

现象：

```text
Downloading ... .insightface/models/buffalo_l.zip
```

说明：这是 InsightFace 自动下载人脸检测、landmark、识别等 ONNX 模型，主要用于人脸分析或 LivePortrait 相关预处理。首次下载很慢是正常的。

如果下载完成，后续不会重复下。

## 22. LivePortrait `Invalid device string: 'cuda:None'`

现象：

```text
Frame rendering failed: Failed to initialise LivePortrait pipeline: Invalid device string: 'cuda:None'
```

含义：LivePortrait 已经不再 fallback，开始加载真实权重，但 wrapper 给官方 `InferenceConfig` 传了 `device_id=None`，上游内部拼成了非法设备字符串 `cuda:None`。

解决：同步新版：

```text
src/modules/meme_animator/live_portrait.py
```

新版会把：

```python
torch.device("cuda") -> device_id=0
torch.device("cuda:2") -> device_id=2
```

同步后验证：

```bash
grep -n "_liveportrait_device_id\\|device_id=" src/modules/meme_animator/live_portrait.py
python scripts/smoke_test.py
```

如果只剩：

```text
ToonCrafter weights not found ...
```

这是可以接受的，说明 LivePortrait、SAM、TripoSR 已经在真实路径上继续向前跑。

## 23. LivePortrait `'NoneType' object has no attribute 'insightface_root'`

现象：

```text
Failed to initialise LivePortrait pipeline: 'NoneType' object has no attribute 'insightface_root'
```

含义：LivePortrait 五个主权重已经加载成功，但 wrapper 创建官方 `LivePortraitPipeline` 时传了 `crop_cfg=None`。官方 pipeline 初始化 cropper 时会读取：

```text
crop_cfg.insightface_root
crop_cfg.landmark_ckpt_path
```

因此不能传 `None`。

解决：同步新版：

```text
src/modules/meme_animator/live_portrait.py
```

新版会自动构造 `CropConfig`，并默认使用：

```text
INSIGHTFACE_ROOT 或 ~/.insightface
$LIVE_PORTRAIT_MODEL_PATH/liveportrait/landmark.onnx
```

同步后检查：

```bash
grep -n "CropConfig\|_build_liveportrait_crop_config\|insightface_root" src/modules/meme_animator/live_portrait.py
ls -lh "$LIVE_PORTRAIT_MODEL_PATH/liveportrait/landmark.onnx"
ls -ld "$HOME/.insightface/models/buffalo_l"
python scripts/smoke_test.py
```

如果 `landmark.onnx` 缺失，重新补 LivePortrait 下载：

```bash
HF_ENDPOINT=https://hf-mirror.com huggingface-cli download KwaiVGI/LivePortrait \
  --repo-type model \
  --local-dir "$LIVE_PORTRAIT_MODEL_PATH"
```

## 24. LivePortrait `object has no attribute 'motion_extractor'`

现象：

```text
Frame rendering failed: 'LivePortraitPipeline' object has no attribute 'motion_extractor'
```

含义：已经进入真实 LivePortrait 推理阶段，但 wrapper 对官方 API 的对象层级理解错了。官方 `LivePortraitPipeline` 是编排器，真正的模型调用方法在：

```python
pipeline.live_portrait_wrapper
```

而不是 pipeline 本身。

官方真实调用路径应接近：

```python
source = live_portrait_wrapper.prepare_source(rgb)
kp_info = live_portrait_wrapper.get_kp_info(source)
f_s = live_portrait_wrapper.extract_feature_3d(source)
x_s = live_portrait_wrapper.transform_keypoint(kp_info)
x_d = live_portrait_wrapper.transform_keypoint(driving_info)
out = live_portrait_wrapper.warp_decode(f_s, x_s, x_d)
rgb = live_portrait_wrapper.parse_output(out["out"])[0]
```

解决：同步新版：

```text
src/modules/meme_animator/live_portrait.py
```

新版 wrapper 会：

1. 检测 `pipeline.live_portrait_wrapper`。
2. 对官方路径使用 `prepare_source`、`get_kp_info`、`extract_feature_3d`、`transform_keypoint`、`warp_decode`、`parse_output`。
3. 兼容官方 `exp` 为 `(1, 21, 3)` 的形状。
4. 保留旧 mock / fallback 路径。

验证：

```bash
grep -n "live_portrait_wrapper\\|extract_feature_3d\\|transform_keypoint\\|warp_decode" src/modules/meme_animator/live_portrait.py
python scripts/smoke_test.py
```

## 25. Albumentations 检查更新超时

现象：

```text
albumentations/check_version.py: Error fetching version info
```

这是联网检查新版本失败，不影响主流程。

可忽略，或设置：

```bash
export NO_ALBUMENTATIONS_UPDATE=1
```

## 26. 推荐验收顺序

不要一上来跑全部模型。建议：

1. 验证环境：

```bash
python - <<'PY'
import cv2, fastapi, numpy, PIL, torch, trimesh
print("base deps OK")
print("torch cuda:", torch.cuda.is_available())
PY
```

2. 验证第三方 import。

3. 验证 TripoSR 权重：

```bash
ls -lh "$TRIPOSR_MODEL_PATH/config.yaml" "$TRIPOSR_MODEL_PATH/model.ckpt"
```

4. 验证 LivePortrait 权重。

5. 验证 SAM 权重。

6. 跑 smoke test：

```bash
cd ~/workspace/emoSVG
python scripts/smoke_test.py
```

7. 启动 API：

```bash
uvicorn src.api.main:app --host 0.0.0.0 --port 8000 --workers 1
```

8. 另开终端测试：

```bash
curl http://127.0.0.1:8000/health
curl http://127.0.0.1:8000/status
```

9. 用真实角色图先跑最小链路：

```bash
curl -X POST http://127.0.0.1:8000/generate \
  -F "file=@/data3/zhengmuhan/path/to/your_character.png" \
  -F "expression=shock" \
  -F "output_format=gif" \
  -F "run_3d=false" \
  -F "run_svg=false" \
  -F "use_toon_crafter=false" \
  -F "fps=12" \
  -F "width=512" \
  -F "height=512"
```

10. 再逐步打开 SVG、3D、ToonCrafter。

## 27. 最重要的判断原则

1. `import OK` 只说明源码路径接好了，不代表权重下载好了。
2. `smoke test passed` 只说明管线能跑通，不代表所有真实模型都启用了。
3. `Done. Model weights are in ...` 只说明脚本结束，不代表下载成功。
4. 判断权重是否成功，只看真实文件是否存在。
5. ToonCrafter 最后再处理，不要让它污染主环境。
6. 每次新开终端都要：

```bash
conda activate EmoSVG
cd ~/workspace/emoSVG
set -a
source .env
set +a
```

## 28. ToonCrafter 权重和配置下载

现象：

```text
ToonCrafter weights not found ...
```

或者下载脚本只拿到了 `model.ckpt`，但没有 `config.yaml`。

含义：这不一定是下载失败。ToonCrafter 和 TripoSR/LivePortrait 不太一样，官方模型仓库主要放大权重，推理配置常在官方 Space/GitHub 代码里。emoSVG 当前期望：

```text
$TOON_CRAFTER_MODEL_PATH/model.ckpt
$TOON_CRAFTER_MODEL_PATH/config.yaml
```

新版 `scripts/download_models.py` 已经把 ToonCrafter 处理成两步：

1. 从 `Doubiiu/ToonCrafter` 下载 `model.ckpt`。
2. 从 `Doubiiu/tooncrafter` 的 Space 或 GitHub raw 下载 `configs/inference_512_v1.0.yaml`，再复制为 `config.yaml`。

推荐命令：

```bash
conda activate EmoSVG
cd ~/workspace/emoSVG
set -a
source .env
set +a

export HF_ENDPOINT=https://hf-mirror.com
export HF_HOME=/data3/zhengmuhan/workspace/emosvg_models/.hf_cache
python scripts/download_models.py --models toon_crafter
```

验收：

```bash
ls -lh "$TOON_CRAFTER_MODEL_PATH/model.ckpt"
ls -lh "$TOON_CRAFTER_MODEL_PATH/config.yaml"
ls -lh "$TOON_CRAFTER_MODEL_PATH/configs/inference_512_v1.0.yaml"
ls -lh "$TOON_CRAFTER_MODEL_PATH/checkpoints/tooncrafter_512_interp_v1/model.ckpt"
```

如果脚本失败，手动下载：

```bash
mkdir -p "$TOON_CRAFTER_MODEL_PATH"

HF_ENDPOINT=https://hf-mirror.com huggingface-cli download Doubiiu/ToonCrafter \
  model.ckpt \
  --repo-type model \
  --local-dir "$TOON_CRAFTER_MODEL_PATH" \
  --resume-download

HF_ENDPOINT=https://hf-mirror.com huggingface-cli download Doubiiu/tooncrafter \
  configs/inference_512_v1.0.yaml \
  --repo-type space \
  --local-dir "$TOON_CRAFTER_MODEL_PATH/_space" \
  --resume-download

cp "$TOON_CRAFTER_MODEL_PATH/_space/configs/inference_512_v1.0.yaml" \
   "$TOON_CRAFTER_MODEL_PATH/config.yaml"
```

如果 Space 下载失败，用 GitHub 配置兜底：

```bash
wget -O "$TOON_CRAFTER_MODEL_PATH/config.yaml" \
  https://raw.githubusercontent.com/Doubiiu/ToonCrafter/main/configs/inference_512_v1.0.yaml
```

重要判断：

```text
import lvdm OK
```

只说明 ToonCrafter 官方源码路径已经接进 Python 环境，不代表 emoSVG 的 `ToonCrafterWrapper` 已经能真实调用官方推理。当前 wrapper 仍可能需要按官方 `lvdm` 推理脚本再做一层 adapter。第一步先把权重和配置下载齐，再处理 adapter。

## 29. ToonCrafter 官方脚本跑通后，API 仍不是真 ToonCrafter

现象：

```text
ToonCrafter smoothing failed (ToonCrafter package not installed...)
```

或者 `/animate use_toon_crafter=true` 很快返回，并且没有明显加载 10GB checkpoint / 显存上涨。

原因：旧版 `ToonCrafterWrapper` 期待：

```python
from tooncrafter.inference import ToonCrafterInference
```

但当前官方 ToonCrafter 可用入口是：

```bash
python scripts/evaluation/inference.py ...
```

解决：同步新版：

```text
src/modules/meme_animator/toon_crafter.py
src/modules/meme_animator/animator.py
```

新版会：

1. 把 LivePortrait 输出的相邻帧写成 ToonCrafter 官方 `prompt_dir`。
2. 一次 subprocess 调 `scripts/evaluation/inference.py` 批量处理帧对。
3. 从官方输出 mp4 读回帧，返回给 emoSVG。
4. ToonCrafter 失败时返回 `toon_crafter_failed`，不再误报成功。

建议 `.env` 中加入：

```bash
TOON_CRAFTER_REPO_PATH=/data3/zhengmuhan/workspace/third_party/ToonCrafter
TOON_CRAFTER_MAX_SIDE=256
TOON_CRAFTER_DDIM_STEPS=25
TOON_CRAFTER_VIDEO_LENGTH=16
TOON_CRAFTER_PERFRAME_AE=1
TOON_CRAFTER_TIMEOUT_SEC=1800
```

如果日志出现：

```text
torch.cuda.OutOfMemoryError
Running official ToonCrafter: 20 pair(s), 512x512, 16 frames
```

说明已经真正调用了 ToonCrafter，但内部推理分辨率太高。设置 `TOON_CRAFTER_MAX_SIDE=256` 后，新版 adapter 会默认降采样给 ToonCrafter，再把结果缩回 API 输出分辨率。

重启 API 后测试：

```bash
curl -X POST http://127.0.0.1:8000/animate \
  -F "file=@/data3/zhengmuhan/workspace/emoSVG/test_data/1.png" \
  -F "expression=shock" \
  -F "output_format=gif" \
  -F "use_toon_crafter=true" \
  -F "frames_between=1" \
  -F "fps=8" \
  -F "width=256" \
  -F "height=256"
```

成功标志：

```text
Running official ToonCrafter
>>> model checkpoint loaded.
ToonCrafter smoothing done
backend_used=live_portrait+toon_crafter[interpolator]
```

## 30. `.env` 中写了 `TOON_CRAFTER_REPO_PATH`，但 Python 读到是 `None`

现象：

```bash
python - <<'PY'
import os
print(os.getenv("TOON_CRAFTER_REPO_PATH"))
PY
```

输出：

```text
None
```

或者 API 日志里出现：

```text
Failed to initialise ToonCrafter official adapter:
expected str, bytes or os.PathLike object, not NoneType
```

原因：`.env` 没有在启动 `uvicorn` 的那个终端加载，或者 `.env` 写法不符合 shell 语法。

正确写法不能有空格：

```bash
TOON_CRAFTER_REPO_PATH=/data3/zhengmuhan/workspace/third_party/ToonCrafter
```

错误写法：

```bash
TOON_CRAFTER_REPO_PATH = /data3/zhengmuhan/workspace/third_party/ToonCrafter
```

验证：

```bash
cd ~/workspace/emoSVG
set -a
source .env
set +a

python - <<'PY'
import os
from pathlib import Path
p = os.getenv("TOON_CRAFTER_REPO_PATH")
print("TOON_CRAFTER_REPO_PATH =", repr(p))
print("exists =", Path(p).exists() if p else None)
print("inference.py =", (Path(p) / "scripts/evaluation/inference.py").exists() if p else None)
PY
```

期望：

```text
exists = True
inference.py = True
```

每次改 `.env` 后都要重启 `uvicorn`，旧进程不会自动继承新变量。

## 31. ToonCrafter 已真正启动，但 `CUDA out of memory`

现象：

```text
Running official ToonCrafter: 20 pair(s), 512x512, 16 frames
>>> model checkpoint loaded.
torch.cuda.OutOfMemoryError
```

含义：这说明路径、权重、adapter 都已经通了；问题变成真实 ToonCrafter 推理负载过大。

解决：先用保守参数跑通。

```bash
TOON_CRAFTER_MAX_SIDE=256
TOON_CRAFTER_VIDEO_LENGTH=8
TOON_CRAFTER_DDIM_STEPS=10
TOON_CRAFTER_PERFRAME_AE=1
PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True
```

重启 API 后成功日志应类似：

```text
Running official ToonCrafter: 20 pair(s), 256x256, 8 frames, 10 DDIM steps.
>>> model checkpoint loaded.
Saved in ...
Official ToonCrafter completed ...
ToonCrafter smoothing done: 41 total frames
```

如果仍 OOM，再进一步降：

```bash
TOON_CRAFTER_VIDEO_LENGTH=4
TOON_CRAFTER_DDIM_STEPS=5
```

或者后续把 adapter 改成分批处理帧对。

## 32. ToonCrafter 官方脚本缺运行依赖

现象和处理：

```text
ModuleNotFoundError: No module named 'pytorch_lightning'
```

```bash
pip install --no-deps pytorch-lightning==1.9.3 torchmetrics==0.11.4 lightning-utilities==0.15.2 \
  -i https://pypi.tuna.tsinghua.edu.cn/simple
```

```text
ModuleNotFoundError: No module named 'pkg_resources'
```

```bash
pip install setuptools==69.5.1 -i https://pypi.tuna.tsinghua.edu.cn/simple
```

```text
ModuleNotFoundError: No module named 'open_clip'
```

```bash
pip install --no-deps open_clip_torch==2.22.0 -i https://pypi.tuna.tsinghua.edu.cn/simple
pip install --no-deps ftfy sentencepiece "protobuf>=4.25.3,<5" -i https://pypi.tuna.tsinghua.edu.cn/simple
```

```text
NameError: name 'xformers' is not defined
```

先确认 PyTorch：

```bash
python - <<'PY'
import torch
print(torch.__version__)
print(torch.version.cuda)
PY
```

如果是 `torch==2.3.0` / CUDA 12.1，安装：

```bash
pip install --no-deps xformers==0.0.26.post1 -i https://pypi.tuna.tsinghua.edu.cn/simple
```

验证：

```bash
python - <<'PY'
import torch
import xformers
import xformers.ops
q = torch.randn(1, 64, 8, 40, device="cuda", dtype=torch.float16)
out = xformers.ops.memory_efficient_attention(q, q, q)
print("xformers attention OK:", out.shape)
PY
```

```text
ImportError: PyAV is not installed
```

```bash
pip install av==12.3.0 -i https://pypi.tuna.tsinghua.edu.cn/simple
```

原则：缺什么小包补什么，不要执行 ToonCrafter 全量 requirements，避免污染 `torch/numpy/opencv/transformers`。

## 33. `curl` 命令换行反斜杠后有空格

现象：

```text
curl: (3) URL using bad/illegal format or missing URL
-F: command not found
```

原因：这一行的 `\` 后面多了空格：

```bash
-F "file=@xxx.png" \
```

正确写法：反斜杠必须是行尾最后一个字符。

也可以直接用一行版本：

```bash
curl -X POST http://127.0.0.1:8000/animate -F "file=@/data3/zhengmuhan/workspace/emoSVG/test_data/1.png" -F "expression=shock" -F "output_format=gif" -F "use_toon_crafter=true" -F "frames_between=1" -F "fps=8" -F "width=256" -F "height=256"
```

## 34. 判断 ToonCrafter 是否真的接入成功

不成功但 API 仍可能返回 200，因为 emoSVG 会回退到 unsmoothed LivePortrait。

失败/回退标志：

```text
ToonCrafter smoothing failed ...
using unsmoothed frames
backend_used=live_portrait+toon_crafter_failed[interpolator]
```

真实成功标志：

```text
Running official ToonCrafter
>>> model checkpoint loaded.
Prompts testing [rank:0] 20/20 samples loaded.
Saved in ...
Official ToonCrafter completed ...
ToonCrafter smoothing done: 41 total frames
```

成功结果：

```text
21 keyframes -> 41 total frames
backend_used=live_portrait+toon_crafter[interpolator]
```
